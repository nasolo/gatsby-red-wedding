import styled from "styled-components";
import tw from 'twin.macro'



export const LeftControls = styled.div.attrs({
    className: 'left-controls'
})`

    ${
        tw`
            flex
        `
    }

    .icon{
        ${
            tw`
                m-auto
            `
        }
    }

`

